# Never index the admin panel or the API surface. This is discoverability
# hygiene, not access control — both are enforced server-side.
#
# NOTE: a crawler obeys ONLY the most specific User-agent group that matches
# it, so every named group below must repeat the Disallow rules — a named
# group with just "Allow: /" would let that bot crawl /admin and /api/.

User-agent: *
Allow: /
Disallow: /admin
Disallow: /admin/
Disallow: /api/

# AI + search crawlers, explicitly welcomed (same rules as everyone else)
User-agent: GPTBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-Web
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: PerplexityBot
User-agent: cohere-ai
User-agent: facebookexternalhit
User-agent: Bingbot
User-agent: Googlebot
Allow: /
Disallow: /admin
Disallow: /admin/
Disallow: /api/

Sitemap: https://laraspinal.in/sitemap.xml
